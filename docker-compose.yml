services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: vol_user
      POSTGRES_PASSWORD: vol_pass
      POSTGRES_DB: vol_surface
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
